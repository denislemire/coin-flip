version: 2.1
orbs:
  # Your orb will be automatically injected here during the pipeline.
  # Reference your orb's jobs and commands below as they will exist when built.
  orb-tools: circleci/orb-tools@12.0
  aws-cli: circleci/aws-cli@5.1.2
  # The orb definition is intentionally not included here. It will be injected into the pipeline.
  coin-flip: {}

# Use this tag to ensure test jobs always run,
# even though the downstream publish job will only run on release tags.
filters: &filters
  tags:
    only: /.*/

# Filter for release tags.
release-filters: &release-filters
  branches:
    ignore: /.*/
  tags:
    only: /^v[0-9]+\.[0-9]+\.[0-9]+$/

jobs:
  # Create jobs to test the commands of your orbs.
  # You may want to add additional validation steps to ensure the commands are working as expected.
  command-test:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      # Run your orb's commands to validate them.
      - coin-flip/greet
workflows:
  test-deploy:
    jobs:
      - aws-cli/setup:
          role_arn: arn:aws:iam::955620237348:role/CircleCI-OIDC-Role
          profile_name: "OIDC-PROFILE"
      - aws-cli/setup:
          role_arn: arn:aws:iam::992382483259:role/denisl-oidc-s3
          profile_name: "OIDC-CCI-PROFILE"
      - run:
          name: Get CircleCI token from secrets manager
          command: |
            export CIRCLE_TOKEN=`aws --profile OIDC-CCI-PROFILE secretsmanager get-secret-value --secret-id circle-token | jq -r '.SecretString | fromjson | .CIRCLE_TOKEN'`
            echo ${CIRCLE_TOKEN} | circleci context store-secret --org-id ${CIRCLE_ORGANIZATION_ID} orb-publishing CIRCLE_TOKEN
      # Make sure to include "filters: *filters" in every test job you want to run as part of your deployment.
      # Test your orb's commands in a custom job and test your orb's jobs directly as a part of this workflow.
      - command-test:
          filters: *filters
      # The orb must be re-packed for publishing, and saved to the workspace.
      - orb-tools/pack:
          filters: *release-filters
      - orb-tools/publish:
          orb_name: denislemire/coin-flip
          vcs_type: << pipeline.project.type >>
          pub_type: production
          # Ensure this job requires all test jobs and the pack job.
          requires:
            - orb-tools/pack
            - command-test
          context: orb-publishing
          filters: *release-filters
      - run:
          name: Remove token
          command: |
            circleci context remove-secret --org-id ${CIRCLE_ORGANIZATION_ID} orb-publishing CIRCLE_TOKEN
